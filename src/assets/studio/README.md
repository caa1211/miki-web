# Studio images

Add approved studio and environment photographs here. Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`, and `.avif`.

The About page discovers this folder at build time and automatically generates responsive WebP images. Sortable filenames control display order.
